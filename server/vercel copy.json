
// новый проба
{
  "routes": [
    { "source": "/(.*)", "destination": "/" },
    { "source": "/objects/(.*)", "destination": "/objects" },
    { "source": "/deals/(.*)", "destination": "/deals" },
    { "source": "/meetings/(.*)", "destination": "/meetings" },
    { "source": "/calendar/(.*)", "destination": "/calendar" },
    { "source": "/ridge/(.*)", "destination": "/ridge" }
  ]
}

// рабочий
{
  "version": 2,
  "builds": [
    {
      "src": "./app.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "./app.js",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"],
      "headers": {
        "Access-Control-Allow-Origin": "*"
      }
    }
  ]
}

// еще такой с форумов есть
{
  "routes": [{
      "src": "/[^.]+",
      "dest": "/",
      "status": 200
  }]
}